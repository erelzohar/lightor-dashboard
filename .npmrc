# react-facebook-login (last published 2018) declares a peer of React 16 and
# breaks every npm install against React 18 (LT-019). Until it is replaced,
# install with the pre-npm-7 peer behaviour. Remove this when it goes.
legacy-peer-deps=true
